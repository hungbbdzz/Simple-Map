## PASS130 — Xaero-style stable Cave writer + previous-layer minimap fallback + branch quiet-window coalescing

- Focused exclusively on the remaining Cave layer-transition pressure from the supplied PASS129 run. Surface, water, The End void handling, lighting and pin/navigation behavior are unchanged in this pass. The latest baseline is already much healthier on exact ownership (`CAVE_REGION_FOREGROUND_HANDOFF_REJECTED=0`, 2,718 exact completions with 243 discarded), but Layered still performs 4,473 tile swaps across only 23 retargets and queues 15,326 branch updates; branch latency still peaks around 10.02 s. The remaining bottleneck is therefore repeated layer/branch refinement rather than missing source data.
- Re-audited Xaero's split between the cave destination, the cave writer being prepared and the cave state currently rendered. `CaveTextureManager` now gives MINIMAP its own stable `minimapWriterLayerY` instead of letting every transient player Top-Y become projection work immediately. A candidate Top-Y must remain unchanged for 100 ms and the player must remain vertically/spatially stable for 180 ms before a cold writer retarget. This mirrors Xaero's `!playerMoving && caveStartOutdated(...)` behavior: movement may change the desired Cave layer, but does not continuously restart the expensive writer. A previously cached exact target can still commit immediately.
- The direct focus projection in `CaveProjectionController` now follows the selected minimap writer layer as well. Previously it could bypass the facade and project one 16x16 focus tile at every transient Top-Y even if the wider viewport writer was intentionally retained. This closes that second writer path so stable-Y debounce applies to both viewport and focus projection work.
- New minimap writer transitions are centre-first. A cold layer commit initially requests only roughly the centre page neighborhood, expands to a near window after the centre is coherent (or 250 ms), then expands to the full minimap viewport after at least 3/5 centre+cardinal pages are coherent (or 700 ms). This follows Xaero's reduced writer distance during cave transitions: the area the player actually looks at becomes useful first instead of flooding the full minimap rectangle at once. Telemetry: `CAVE_MINIMAP_WRITER_LAYER_COMMIT` and `CAVE_MINIMAP_WRITER_WINDOW_EXPANDED`.
- Added a true previous-layer visual fallback for the minimap. On a cross-band transition, current-layer exact/branch data is preferred, then the stable writer layer, then one retained previous writer layer. At exact zoom, a missing current logical leaf gets the previous-layer tile as `PHASE_L1_EXACT_UNDERLAY` while the current-layer logical page-table key is still emitted above it. As soon as the current page becomes resident it replaces the underlay without rebuilding the immutable plan. This is the same user-facing trick as Xaero's `previousRenderedCaveLayer`: the minimap remains filled while the new layer progressively replaces old tiles.
- Minimap render-plan identity for Layered Cave is now the normalized 16-block band rather than every exact Top-Y. Y=48..63 therefore shares one minimap geometry/plan authority; exact Top-Y remains a content/projection revision inside that band. Fullscreen/manual Cave remains exact-Top-Y keyed. This removes plan/FBO authority churn when walking stairs or slopes within one retained Cave band.
- Added branch quiet-window coalescing to `CaveLodTree`. Exact pages remain progressive, but an already-established branch no longer reduces/uploads once per child/page revision burst. MINIMAP branch inputs wait for a 90 ms quiet period with a 180 ms hard hold cap (fullscreen/background use their own shorter/longer limits); newer revisions replace the same pending page snapshot in-place. Cold branch seeds remain immediately eligible. `branch_updates_queued` now represents unique queue admissions rather than every superseding revision, and `CAVE_BRANCH_PAGE_COALESCED` exposes absorbed bursts.
- Branch queue telemetry keeps the real first-enqueue timestamp; cold immediate eligibility is represented explicitly rather than by falsifying enqueue time. This preserves meaningful `latency_branch_queue_*` measurements for the next comparison. No worker-pool, GPU-budget or Cave-resolution increase was made. The intended gain is lower work amplification and better frame pacing, not more brute-force throughput.
- Validation: 250 production Java files; comment/string-aware delimiter scan reports 0 issues. `CAVE_PASS90_PROJECTION_WORKING_SET_PASS`, `CAVE_PASS129_PLANNER_OWNERSHIP_PASS`, and new `CAVE_PASS130_XAERO_STABLE_LAYER_PASS` pass. Java 21 front-end probing finds no syntax/edit-signature diagnostics before the expected missing Minecraft/NeoForge/FastUtil/Log4j classpath errors. Full Gradle remains unavailable because the supplied source bundle lacks the wrapper JAR.

## PASS129 — live block-anchored navigation + retained minimap marker transform + Cave planner-owned exact work

- Corrected PASS128's navigation interpretation. The destination remains fixed, but the guide route now always originates from the player's **current block**. The complete player->target world segment is solved before any viewport clipping. Guide dots are phased from the destination backwards at the existing 8-block cadence, then snapped to block centres. Moving within the same block cannot move the lattice at all; entering a new block legitimately recomputes the route. Pan/zoom/offscreen clipping only selects existing dot indices and can no longer redistribute them from a clipped screen endpoint. Legacy persisted route-origin fields remain readable for compatibility but are no longer navigation authority, and the old per-tick route-origin capture/save hook has been removed.
- Fixed the remaining minimap pin jitter at its actual composition boundary. PASS128 projected the pin independently from live player delta while the terrain itself could reuse a snapped retained FBO crop. Even mathematically equivalent transforms can differ by a fractional/one-pixel phase. `MinimapFramebufferRenderer` now exposes a read-only `projectWorldToHud()` using the exact frame currently displayed: snapped world anchor, target scale, crop source, physical/logical display span and final yaw. Pin and guide dots use that same transform but remain live overlays; they are never baked into the retained texture. Marker/text/dots are rendered at fractional HUD coordinates instead of `Math.round()` anchors. A direct live world transform remains fallback if no valid retained composition exists.
- Latest PASS128 telemetry confirms some Cave gains but substantial derived-work pressure remains. Main Nether GAME Layered improves to ~9.17 ms average and Full to ~9.62 ms; native foreground handoff rejects fall to **0** in that Nether session. However branch work still queues 7,118 updates, sees 2,315 additional GPU branch denials and reaches ~21.94 s max queue age. Overworld still records 339 native foreground handoff rejects plus 96 `CAVE_RESULT_STALE`; Nether records 63 `CAVE_RESULT_STALE`.
- The 339 remaining Overworld native-region rejects expose a different transition bug from PASS128's lane handoff: every sampled reject is an old `FULLSCREEN` projection offer, concentrated when the fullscreen Cave view changes (for example LAYERED -> FULL). `activateViewport()` previously retired only region requests that matched the **new** projection key, so the old view/Top-Y writer lease survived long enough to emit a stale wave. PASS129 makes each foreground lane own one active presentation projection: switching view/exact Top-Y immediately clears the superseded region foreground mask and queued offers. An already-running CPU slice may finish into the retained projected-page cache, but it is marked `presentationRetired` and is not requeued or published into the new view. New telemetry: `CAVE_REGION_SUPERSEDED_PROJECTION_RETIRED`.
- Found a more fundamental Cave exact-lifetime mismatch with Xaero. SimpleMap's minimap `PageRequest` lease is intentionally short (~2 s), but `publishCompleted()` and the generic exact-build cancellation token still treated that transient request object as the only owner. The visible planner/writer window can remain valid much longer, especially while branch/exact queues are under pressure. A build could therefore finish for a page that is still visibly required and be discarded only because its request lease expired. Xaero separates the loading/loaded writer window from a short scheduling pulse; PASS129 follows that model.
- Exact Cave pending/completion ownership is now `live PageRequest OR current MINIMAP/FULLSCREEN planner` for the same projection. Expiring a short request no longer detaches an in-flight build if the visible planner still owns it; `isPendingStillWantedLocked()` uses the same rule. The worker cancellation token now guards only dimension/source-generation validity—viewport/mode retirement explicitly detaches/cancels obsolete work—so worker threads never race mutable render-thread `VisiblePlanner` bounds. Final publication still performs the authoritative request-or-planner check. New diagnostics: `CAVE_REQUEST_EXPIRED_VIEWPORT_RETAINED` and `CAVE_COMPLETION_NO_LONGER_OWNED`.
- Hidden-lane suspension now clears its planner **before** request pruning/detachment. This preserves the planner-owned lifetime rule without allowing a closed MapScreen/minimap lane to keep exact futures alive accidentally. Detached-future terminal state also recognises viewport ownership, avoiding false `STALE_GENERATION` classification after a valid short request expires.
- Xaero comparison remains consistent with this direction: `MinimapWriter` keeps separate `loadingCaving`/`loadedCaving` state and bounds its writer with both `tilesToUpdate` and a hard time limit; World Map retains `MapTile` written cave state independently from the currently loaded presentation. SimpleMap already has retained 16x16 Cave projection tiles; the current optimization target is therefore lifetime/assembly/publication of the 64x64 presentation page rather than pretending the 16x16 projection layer is absent.
- Validation: 250 production Java files. `MINIMAP_PIN_LIVE_ROUTE_COMPOSITION_ANCHOR_PASS` and new `CAVE_PASS129_PLANNER_OWNERSHIP_PASS` pass as dependency-free source guards. Java 21 front-end probing reports no syntax/edit diagnostics before the expected missing Minecraft/NeoForge/FastUtil/Log4j classpath symbols. Full Gradle is still unavailable because the supplied bundle has no Gradle wrapper JAR.

## PASS128 — Xaero-style stable map lighting + immutable world-space pin routes + Cave viewport/branch retention

- The End known-void Surface behavior from PASS127 is left intact. This pass focuses on the two remaining presentation issues from the supplied screenshots (lighting stability and navigation-pin anchoring) plus the latest Cave telemetry. Surface water/composition is otherwise untouched.
- Re-audited Xaero World Map `MapProcessor.getBrightness()` / `MapDimension.getSkyDarken()`. Xaero derives AUTO brightness from `DimensionType.ambientLight()` with a 0.375 ambient base, respects `forceBrightLightmap()`, and does not make a skylit cave layer pulse through the surface day/night curve. `DimensionMapProfile` is now v2 and persists per-dimension ambient light in addition to skylight/ceiling/force-bright/coordinate-scale metadata. Surface AUTO brightness follows the same ambient + normalized-sun formula; no-skylight dimensions use stable dimension ambient, and Cave AUTO uses dimension ambient instead of an unrelated Overworld sky sample.
- Removed the random per-tick Surface light-probe loop from `ChunkScanner`. Surface chunk transactions already capture block light and `MapMutationBus.onLightUpdate()` provides deterministic chunk/light-packet refreshes. The random sampler could dirty arbitrary stationary map columns and make glow/lighting appear patchy even though no terrain changed. Retained minimap brightness is quantized to 24 stable exposure buckets so tiny time interpolation changes no longer force excessive FBO redraws.
- Navigation guide geometry is now immutable world-space state. A new pin captures one block-centred route origin exactly once and persists it in the dimension `pin.json`. The route is a fixed 8-block lattice from that origin to the destination; viewport clipping/zoom may choose a bounded subset of lattice indices but never redistributes them. Guide dots therefore stay attached to the same blocks when the pin moves offscreen or the full map is panned. Legacy pin files remain compatible and capture/persist their origin once on the first live tick.
- Removed minimap pin authority from the retained framebuffer composition. `projectWorldToScreen()` used a snap-reused FBO anchor that could be stale relative to the live player transform; the supplied behavior—nearby pins clamped to an edge—was consistent with that mismatch. Pin/icon/route now use the direct live world delta -> optional `-yaw-180` rotation -> blocks-to-pixels zoom transform. Only genuinely offscreen pins are edge-clamped. Fixed route dots are projected from their immutable world block centres and are simply clipped at the minimap shape.
- Latest pre-PASS128 Cave telemetry still shows the main pressure in gameplay/minimap rather than MapScreen: Overworld GAME Layered averages ~8.87 ms and Full ~9.36 ms, while Nether GAME Layered averages ~9.55 ms and Full ~9.82 ms. The Nether session adds 11,389 branch updates, 1,090 branch GPU denials and reaches ~21.10 s branch-queue max age. PASS126/127 retention is working—every sampled Layered retarget now reports `retired_projection_entries=0`—so this pass targets remaining stale publication/branch rebuild work rather than undoing that retention.
- All 734 Overworld and all 851 Nether `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` events in the supplied log are `FULLSCREEN` offers and occur in tight transition clusters (for example Nether has 209/198/442 rejects around three transition seconds). PASS127 only called `CaveRegionProjectionService.activateViewport()` for FULLSCREEN, leaving the region producer on the old fullscreen writer window until later importer lease updates. PASS128 fences the native-region producer on both MINIMAP and FULLSCREEN viewport changes, treats lane change itself as a planner change, updates each region request's current foreground lane, and prunes any queued foreground page outside the new viewport before consumer handoff.
- Background native-region Cave projection now waits for a source-coherent central 4x4-chunk page. Partial child waves remain allowed for foreground exact presentation, but background refinement no longer starts a 64x64 projection while sibling source chunks are still arriving. This directly targets the `CAVE_RESULT_STALE` source-fingerprint churn without delaying visible progressive Cave fill.
- Retained Cave branch CPU work now keeps its coherent source stamp when a FULL/LAYERED view or 16-block Layered band is parked. PASS127 retained the CPU LOD pixels but deleted the stamp, so returning to an unchanged view could still feed the same complete page back through `lodTree.updatePage()` and re-derive the hierarchy. Stamps are now removed only when their bounded `PageInfo` is actually evicted (same-band exact Top-Y replacement still removes the obsolete exact-projection stamp). This follows Xaero's retained written-tile behavior and targets branch queue/re-derive churn without increasing GPU residency.
- Validation: 250 production Java files pass a comment/string-aware delimiter scan. Updated `MinimapPinCompositionAnchorCheck` passes as `MINIMAP_PIN_WORLD_ANCHOR_PASS`; `CavePass90ProjectionWorkingSetCheck` passes with PASS128 viewport/background-coherence/retained-branch invariants. Java 21 front-end probing reports zero syntax-like diagnostics; remaining compilation errors are the expected missing Minecraft/NeoForge/FastUtil/Log4j classpath symbols in the supplied standalone source bundle. Full Gradle remains unavailable because the bundle has `gradlew.bat` but no Gradle wrapper JAR.

## PASS127 — Xaero known-void Surface semantics + Cave native single-writer / coherent-branch publication

- Re-audited the supplied PASS126 run, the End screenshot and Xaero World Map's `MapWriter` / `MapPixel` instead of treating the black End gaps as a Cave issue. Xaero distinguishes a mapped air-only column from an undiscovered column: when `MapWriter.loadPixel()` finds no opaque surface it writes `Blocks.AIR` at the dimension bottom, and `MapPixel` uses `VOID_COLOR = 0xFF0A0017`. Known End void is therefore a real opaque dark-void map pixel; missing/unwritten map data remains absent.
- SimpleMap already carried the same semantic distinction in its Surface source: `RegionWindow.known` is chunk-completion coverage and `MapTextureBuildWorker` explicitly documents that a scanned void column is still known. The style stage nevertheless did `MapBlockData.isEmpty(packed) -> transparent`, collapsing known void and unknown data into the same black/transparent hole. PASS127 preserves coverage through exact and sampled-LOD colorization and maps `known && empty` to Xaero's void colour (converted to NativeImage ABGR). Unknown/unvisited data stays transparent. No Surface source-format migration is needed.
- Versioned only the Surface branch-cache namespace (`surface_chunk16_*` -> `surface_v2_chunk16_*`) so zoomed-out End regions cannot reload pre-PASS127 transparent-void branches. The shared SLOD file format and Cave branch namespace are left intact, avoiding an unnecessary cold Cave-cache rebuild during this Cave-focused pass. The canonical `.smdat` Surface source is unchanged because its existing known/completion coverage was already sufficient.
- The End remains Surface-first. `hasSkyLight=false` is not a Cave predicate: Nether is dark *and* has a hard ceiling, while The End is dark and open to the void. PASS125's dimension profile/hard-ceiling policy remains the authority; PASS127 only fixes how an already-known empty Surface column is presented. Surface water/composition is otherwise untouched.
- PASS126's retained-layer work is confirmed by the latest telemetry: normal Layered retargets now report `retired_projection_entries=0` and some revisits reuse retained pages. The remaining main-Nether bottleneck is still publication/work duplication: GAME Layered averages about 9.35 ms and GAME Full about 10.49 ms; the run adds 2,360 exact-build discards, 2,568 completed-task discards, 208 cancellations, 6,163 branch updates and 2,443 branch GPU denials, with branch-queue max age reaching ~39.28 s.
- All 198 sampled `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` events in that Nether session have `request_owned=false planner_owned=false projection_owned=false`: completed region pages were still leaving the producer queue after their writer window had moved, only to be rejected by the consumer. `CaveRegionProjectionService.pollReady()` now validates the current region writer lease, foreground bitmask and source revision before handoff, prunes stale offers producer-side, and relabels valid MINIMAP/FULLSCREEN offers to the current writer lane. New telemetry: `CAVE_REGION_FOREGROUND_QUEUE_STALE_PRUNED`.
- Closed a remaining dual-writer race. The native 8x8-page region transaction can already publish stable partial child waves, but PASS126 suppressed the generic 64x64 exact resolver only after all 16 child chunks were complete. During incremental loading both paths therefore projected/styled the same page concurrently. The latest run records 104 `CAVE_RESULT_STALE` events where the source fingerprint changed under such exact work. PASS127 treats either the native import viewport demand or its active projection-writer lease as the single CPU projection authority for any resident source; the generic exact path is fallback only when neither native stage owns that page.
- Coarse Cave hierarchy publication is now coherent-page driven. Region-native partial pages can still refine the exact atlas immediately, but they no longer regenerate L1/L2/L3 on every arriving child wave. Only a complete immutable projected page updates branch authority. This matches the existing exact `apply()` rule that branches derive from an authoritative projection and targets the observed 6,163 queued branch updates / ~39 s branch queue without reducing exact Cave quality.
- Region-import supersession now uses the queue-aware `detachPendingLocked()` path. If a newer native projected page replaces an exact future whose completion already reached `completedBuilds`, that stale completion ownership record is removed immediately instead of surviving until a later publication scan and incrementing completed-task discard churn.
- No Cave resolution reduction, worker-pool increase or GPU-budget increase. The pass removes duplicate/stale work and moves publication rejection to the producer, following Xaero's one-writer retained-tile model more closely.
- Validation: 249 production Java files pass a comment/string-aware delimiter scan; the dependency-free `CavePass90ProjectionWorkingSetCheck` passes with PASS127 single-writer/queue/void invariants. A Java 21 front-end probe of the modified production units reports no parser, constructor-signature or edit diagnostics before the expected missing Minecraft/NeoForge/FastUtil/Log4j classpath symbols. Full Gradle remains unavailable because the supplied source bundle has `gradlew.bat` but no wrapper JAR.

## PASS126 — Xaero-style lazy Cave layers + loaded/last-good presentation + writer-window retirement

- Cave-only pass. Surface/water rendering is intentionally untouched.
- Re-audited the latest PASS125 validation log together with Xaero Minimap `MinimapWriter` and World Map `MapWriter` / `LayeredRegionManager`. Xaero's key behavior is not merely a 16-block cave layer: it separates `loadingCaving` from `loadedCaving`, keeps already-written tiles/regions resident while a new cave start is prepared, processes a player-centred writer window under both tile-count and nanosecond limits, and lets bounded loaded-region caches age old layers out rather than eagerly deleting them on every Y change.
- The main Nether session still shows substantial derived-work churn before PASS126: 2,611 exact-build discards, 2,947 completed-task discards, 336 cancellations, 9,455 branch updates, 2,400 branch GPU reservation denials, 104,271 Layered projection hits and 27,265 misses. GAME Layered averages about 9.10 ms in the sampled metrics. The branch queue reaches roughly 30.49 s maximum age even though branch derive/upload themselves are cheap, pointing to ownership/churn rather than raw branch math.
- PASS125 Layered retarget telemetry is the clearest remaining structural problem: 13 Nether retargets parked 2,988 pages but still retired 8,567 completed projection-cache entries, including 914/707/1,406/1,578/1,970/1,992 entries when revisiting an already-known 16-block band with a different exact Top-Y. `reused_retained_pages` remained 0. Xaero does not synchronously destroy an entire `MapLayer` when its exact cave start changes; regions with an old cave start remain valid last-good data and become lazily outdated.
- `CaveBandCache.retainExactProjectionForBand()` is therefore now intentionally non-destructive. Exact Top-Y already participates in the cache key, so multiple recent exact starts inside one 16-block band can safely coexist until the access-order LRU evicts them. `CaveProjectionServiceV2.activateLayeredTopY()` still advances its epoch to fence stale workers but no longer scans/deletes every other exact product in the active band.
- Fixed a second contradiction in the exact-page layer. `PageInfo.beginProjectionTransition()` was documented as preserving the currently visible page, but the normal retarget path immediately called `discardVisibleProjectionForRetarget()`, releasing the atlas slot and clearing visible-tile masks. PASS126 keeps the published same-band page as a visual last-good underlay while the new exact Top-Y is staged. `canRenderProjection()` remains the strict completion/authority gate; `canRenderLastGoodWithinBand()` is render-only and cannot satisfy a new request.
- Layered PageInfo retention is now bounded-all-bands rather than active/previous-band deletion. Inactive bands lose request/pending/publication ownership and exact GPU residency, but coherent CPU `frontLods`/revision products remain in the existing `MAX_PAGES` access-order cache. Returning to a recent band can restore retained CPU work instead of resolve/style/downsample from the archive again. No Cave atlas budget is increased.
- Cave LOD branches now follow the same retained-work rule. Mode or layer changes call `parkInactiveViews()` / `parkLayer()`: branch GPU slots, residency and scheduling queues are released, while bounded Node pixels, child revisions and known/complete masks remain in memory. Returning to the view/layer can republish the hierarchy instead of re-deriving every ancestor. Same-band exact Top-Y changes keep the old branch as last-good and let new exact children replace it incrementally.
- The supplied Nether run still emitted 626 `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` events. Every sampled rejection had both request/planner ownership false, meaning immutable region pages were often offered after their writer window had already moved. PASS126 prunes queued foreground offers when a region mask shrinks and explicitly retires a region's foreground publication lease when the native importer moves that region outside the current viewport. The projected page itself remains in the bounded LRU, so this removes producer/consumer ping-pong without throwing away useful CPU work. New telemetry: `CAVE_REGION_FOREGROUND_READY_PRUNED` and `CAVE_REGION_FOREGROUND_REGION_RETIRED`.
- Native-region projection work is now focus-first inside each 8x8-page region. PASS125 stored `focusPageX/Z` but still processed pending ordinals in region top-left scanline order. PASS126 orders the page slice by squared distance to the current focus page with ordinal only as a stable tie-breaker, and folds the same distance into page priority. This follows Xaero's small player-centred cave writer window and makes the visible centre win when a region contains more ready work than the current slice budget.
- Removed an avoidable allocation path in `CaveRegionProjectionService.ProjectedPage`. Region projection already produces freshly owned styled-pixel and known-row arrays; the record constructor then defensively copied them, and every lane promotion copied the same ~16 KiB page again. Internal ProjectedPage now owns those immutable arrays by reference while its public bulk accessors still return defensive copies. Priority/lane promotion can therefore reuse the same retained payload, closer to Xaero changing tile writer metadata without cloning the written tile.
- Full/Layered dimension isolation, custom-dimension metadata, coordinate scaling and hard-ceiling/open-dimension policy from PASS125 remain unchanged.
- Validation: 249 production Java files pass a comment/string-aware delimiter scan. The dependency-free Cave projection working-set regression test passes with lazy same-band retention, last-good exact-page rendering, branch parking, focus ordering and viewport-retirement guards. A Java 21 front-end probe of the modified production units reports only expected missing Minecraft/NeoForge/FastUtil/Log4j classpath symbols; no parser/edit diagnostics were introduced. Full Gradle build remains unavailable because the supplied source bundle contains `gradlew.bat` but no Gradle wrapper JAR.

## PASS125 — Xaero-style per-dimension map ownership + dimension-qualified Cave archive/projections + custom-dimension metadata

- Re-audited Xaero's dimension architecture rather than treating Overworld/Nether/End as render-mode special cases. `MapWorld` owns a separate `MapDimension` for every `ResourceKey<Level>`; each `MapDimension` owns its own `LayeredRegionManager`, persisted cave-mode type, DimensionType/effects metadata and coordinate scale. `MapProcessor.getCaveLayer()` keys ordinary cave layers by `caveStart >> 4`, so changing Y selects/updates a retained layer instead of globally destroying every other cave layer.
- The latest PASS124 log exposed a more fundamental SimpleMap bug: the compact Cave archive and Cave projection cache were session-global but dimension-blind. Session 1 Overworld ends with 2,531 archive tiles / 5,713 projection entries; the first Nether session starts at 2,590 / 5,771 instead of a fresh Nether namespace. The main Nether session later ends at 6,360 / 11,971 and subsequent Overworld/End sessions inherit those same counters. Same X/Z identities from different dimensions could therefore share archive fingerprints, completeness and derived projections.
- `CaveArchiveV2Service` is now explicitly partitioned by canonical dimension id. Overworld, Nether, End and modded dimensions have independent compact-tile residents, page fingerprints, persistent indexed identities and completeness sets. Only the active dimension owns resident compact tiles; leaving a dimension releases its resident tile memory while retaining lightweight indexed metadata for fast idempotent replay. At most eight inactive dimension partitions are retained, preventing pathological modpacks from growing an unbounded metadata cache.
- Dimension activation happens before Cave repository persistence replay in `MapManager.configureDimension()`. This guarantees that a `.cave`/repository replay can never index Nether tiles into the Overworld archive (or vice versa). True world/session reset still clears every partition; ordinary map-dimension navigation does not.
- `CaveBandCache` and `CaveProjectionServiceV2` are now dimension-qualified. Layered keys are `(dimension, chunkX, chunkZ, exactTopY, archiveRevision, styleRevision)` and Full/coarse identities include dimension as well. A worker captured before a portal/dimension handoff is epoch-fenced, while retained products from inactive dimensions remain safely reusable because their cache identity can no longer alias the active dimension.
- PASS124 still deleted huge amounts of useful projection work when Layered Top-Y crossed a 16-block layer boundary. The supplied run records 16,202 retired projection entries in Overworld and 27,356 in Nether across the sampled retargets — 43,558 completed projection products discarded even though Xaero retains separate cave layers. PASS125 keeps all other 16-block bands in the bounded LRU and only invalidates a superseded exact cave-start inside the same dimension + same 16-block band. Normal A -> B -> A vertical navigation can therefore reuse the old projected band instead of rebuilding it from the archive.
- Added `DimensionMapProfile`, a small persisted per-dimension metadata record analogous to the subset of Xaero's `MapDimension` metadata SimpleMap actually needs: min/max build Y, skylight, hard ceiling, force-bright-lightmap, coordinate scale and whether the type is known. Vanilla Overworld/Nether/End have safe built-in fallbacks; live dimensions overwrite the profile from their real `DimensionType` and build bounds.
- Custom dimensions are no longer assumed to be Overworld-shaped. In singleplayer, an unknown custom map dimension is resolved from the integrated server's actual Level/DimensionType when available and persisted before it is visited through the map. In multiplayer or otherwise unknown remote dimensions, SimpleMap remains conservative until real metadata has been observed, matching Xaero's cache-only behavior when a dimension type cannot be resolved.
- Cave Y bounds now come from the viewed map dimension profile rather than the player's live ClientLevel. The fullscreen Cave slider, manual Top-Y clamping and cached cursor Cave lookup therefore use Nether/custom build limits even while the player is physically in another dimension.
- Cross-dimension map-camera conversion now follows `DimensionType.coordinateScale()` generically instead of hard-coding only Overworld <-> Nether `/8` and `*8`. This preserves the expected Nether 8:1 relationship and also supports modded dimensions with non-1 coordinate scales, mirroring Xaero's `calculateDimScale()`/dimension-divisor model.
- Target-dimension brightness no longer blindly borrows `mc.level.dimensionType()` from the player's live dimension. Remote Nether/End/custom maps use their persisted skylight/ceiling/force-bright profile; live skylit dimensions still use the real sky-darkness value. This prevents an unrelated Overworld day/night/weather state from changing a remotely viewed Nether/End/custom map.
- Fixed a portal-handoff race visible in the latest log: a new Nether session could briefly emit a Cave projection band whose detail still named `dimension=overworld`. If the ClientLevel changes one render frame before `MapManager` switches source namespaces, AUTO/manual Cave is now suppressed for that transient frame instead of rendering old-dimension cave ownership under the new live session.
- Per-dimension behavior is now metadata-driven: Overworld/skylit-open dimensions remain Surface-first with local Layered AUTO Cave; hard-ceiling dimensions such as Nether are Full-capable without sharing Overworld source/projection identity; dark-but-open dimensions such as The End remain Surface-first instead of being mistaken for cave worlds merely because they have no skylight; custom dimensions inherit the same rules from their actual metadata when known.
- Surface-water styling is intentionally untouched. Surface persistence/texture data was already directory/dimension keyed; PASS125 only improves target-dimension metadata, coordinate transforms and lighting so Surface behaves correctly when browsing Nether/End/custom dimensions.
- Latest pre-PASS125 frame baseline remains dimension-dependent: sampled GAME averages are ~8.32 ms in Overworld, ~9.13 ms in Nether (41.85 ms observed peak) and ~7.89 ms in The End. The main expected Cave gain in this pass comes from eliminating cross-dimension aliasing and tens of thousands of unnecessary vertical-band projection retirements rather than increasing worker/GPU budgets.
- Validation: 249 production Java files. Dependency-free Cave projection working-set test passes, including dimension isolation and same-band exact replacement. Java front-end/static checks show no parser/edit diagnostics in the changed code before the expected missing Minecraft/NeoForge classpath symbols. Full Gradle build is unavailable in the supplied source bundle because the Gradle wrapper JAR/Unix launcher is absent.

## PASS124 — Xaero-style retained Cave working sets + minimap CPU fairness + archive batch projection

- Focused exclusively on Cave; the Surface-water artifact is intentionally left unchanged in this pass.
- Latest Nether validation shows the remaining bottleneck is gameplay/minimap Cave rather than fullscreen rendering: GAME Layered averages ~9.15 ms with a 29.73 ms peak, GAME Full ~10.07 ms with a 38.19 ms peak, while MapScreen Layered/Full remain ~8.17/~8.30 ms. Over 166 seconds the run produced 2,316 exact-build discards, 2,488 completed-task discards, 8,124 branch updates, 3,883 branch GPU denials, 21,081 Layered projection misses and 8,405 Full projection builds.
- The strongest churn signal is Layered vertical movement: eight Top-Y retargets retired 1,329 exact pages and 15,832 cached 16x16 projection products. Switching away from Full also retired 7,970 projection entries. PASS123 was still treating a view/band change as cache destruction instead of presentation ownership transfer.
- Re-audited Xaero `MinimapWriter`: `loadingCaving`/`loadedCaving` are distinct, already-written tiles survive state changes, a tile is rewritten only when cave start/depth/content becomes dirty, and each writer pass is bounded by both tile count and a hard nanosecond deadline. PASS124 applies that retained-work principle instead of adding more workers.
- FULL and LAYERED projection products now use independent 8,192-entry LRUs under the same total 16,384-entry budget. Activating Full no longer purges the Layered cache; Layered retains the active and immediately previous exact Top-Y. A stale worker from an older third Top-Y is epoch-fenced from repopulating the retained cache.
- Exact PageInfo products are parked across FULL/LAYERED switches: request/pending ownership and GPU exact residency are revoked, but coherent CPU `frontLods`, source revision and projection identity remain. Returning to the previous view can restore GPU residency directly rather than resolve/style/downsample the page again. Inactive branch GPU hierarchies are explicitly released so retained CPU data does not compete with the active view for branch-atlas space.
- Layered bands now retain two CPU generations (active + immediately previous) instead of closing every non-active band. Returning A -> B -> A reuses authoritative CPU pages when the exact Top-Y matches; only generations older than the previous band are closed. In-flight/completion/backlog ownership from inactive bands is still cancelled so retained data cannot continue consuming foreground scheduling slots.
- `CaveRegionProjectionService` now retains completed immutable projected pages in its existing bounded 4,096-page LRU across Top-Y changes. Only queued/in-flight/ready ownership is retired. Exiting AUTO Cave to Surface (`view=null`) now actually cancels Cave projection queues; PASS123 silently returned without doing so and could leave Cave CPU work running after presentation had switched back to Surface.
- Added Xaero-style gameplay CPU fairness to native-region projection. Fullscreen keeps up to 8 concurrent region jobs with 24 pages per slice, while gameplay/minimap is capped at 4 concurrent jobs with 12 pages per slice and background work at 2 jobs/8 pages. Under governor pressure all region projection collapses to one worker and at most 8 pages per slice. This preserves fullscreen throughput while preventing Cave minimap from occupying all global map CPU workers in one burst.
- Collapsed archive projection lookup lock traffic. A 64x64 Cave page samples a 6x6 chunk window; PASS123 performed 36 synchronized `CaveArchiveV2Service.get()` calls per page even on cache hits. PASS124 snapshots the full 6x6 compact-archive window under one monitor hold and projects from those immutable tile references. This directly targets the latest run's ~59k Layered cache hits + ~21k misses without changing Cave semantics.
- No Cave quality/resolution reduction and no worker-pool increase. The intended gain is less repeated projection, less stale exact work, lower branch/GPU contention and smoother minimap frame pacing while preserving fast fullscreen fill.
- Static front-end validation: 248 production Java files, zero syntax/edit diagnostics before expected missing Minecraft/NeoForge classpath symbols. Full Gradle build is unavailable in the supplied source bundle because the Gradle wrapper JAR/Unix launcher is absent.

## PASS123 — beta water restoration + single Surface authority + deadline-owned Cave handoff

- Compared the supplied old beta SimpleMap source/screenshot directly with PASS122 instead of continuing to infer Xaero's water internals. The beta's known-good ocean uses a compact SimpleMap-specific compositor: water amount grows as `0.25 + depth*0.045` (accurate) / `0.42 + depth*0.035` (vanilla), capped at 0.92/0.94, then the mixed result is attenuated by `pow(0.975, depth-2)` with a 0.46 floor. PASS119-122 had replaced that proven representation with increasingly literal Xaero-style overlay/floor experiments even though SimpleMap stores only one compact water column. PASS123 restores the beta compositor exactly on the current canonical column data.
- Restored the beta water-only 3x3 biome tint kernel. Land keeps the current Xaero-style centre + four-orthogonal blend; liquid uses all nine radius-1 samples, matching the supplied beta implementation that produced the clean ocean screenshot. Water still has no terrain relief or micro-noise.
- Found a new-architecture Surface authority race absent from the beta renderer. `SurfaceWorldSaveReconstructor` could publish a decoded disk snapshot for a chunk that was already a live FULL `LevelChunk`, then call packet-authoritative enqueue and reset that live writer cursor. One chunk could therefore publish disk -> live revisions and dirty exact/halo/LOD work at 16-block cadence. Live FULL chunks now remain the single authority: the disk projection is retained as source cache, a non-resetting live writer nudge is issued, and the disk result is not published.
- Bumped `.smdat` v5 -> v6 with targeted water-only completion invalidation. Existing pixels remain as last-good while only water-bearing chunks are repaired under the restored compositor/single-writer path. Land completion is preserved. Derived branch disk cache v15 -> v16 so stale PASS122 ocean styling cannot return from retained LOD.
- Latest 19:51 log records 9,602 `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` events. The largest class is 5,230 `FULL + FULLSCREEN` pages with `planner_owned=false` but `projection_owned=true`: the short PageRequest had already retired while the active viewport still owned the page. PASS122 nevertheless required that retired request and discarded the region projection. Foreground region handoff now admits when either a live PageRequest OR the active MINIMAP/FULLSCREEN planner owns the same projection; source revision and Layered Top-Y fences remain.
- Applied the same request-or-viewport ownership rule to the immutable region exact backlog for both FULL and LAYERED. A FULL foreground page can no longer be staged while visible and then continue toward exact/branch publication after both request and viewport ownership disappear.
- Fixed a Cave frame-budget violation. `drainRegionProjectedPages()` previously created its own 0.35/1.25 ms deadline without clamping to the outer cave upload deadline, and gameplay/minimap called that importer twice per render frame. PASS123 hard-clamps every import slice to the one outer deadline, limits gameplay staging to at most 48 pages/~0.45 ms, fullscreen to at most 96/~0.9 ms, pressure to ~0.25 ms, and permits a second drain only for fullscreen with measured idle headroom. This follows Xaero's `tilesToUpdate + timeLimit` writer contract instead of stacking independent local budgets.
- Added same-transaction Cave branch GPU denial fencing. Once `CaveLodTree.publish()` is denied by the shared BRANCH upload ledger, later branch publication probes in that same upload transaction are skipped. Dirty retained branches stay queued for a later frame rather than repeatedly probing a budget already known to be exhausted.
- Current measured baseline before PASS123: GAME Surface 8.28 ms average / 24.47 ms peak, GAME Layered Cave 9.00 ms average / 29.14 ms peak; MapScreen Layered Cave 8.75 ms average and Full Cave 8.71 ms. Exact-build discard is already much lower at 266, so this pass targets the remaining ownership/import-budget churn instead of widening worker pools. End-of-run counters include 1,441 cancelled tasks, 1,701 completed-task discards, 248 live Cave requests and 9,602 region handoff rejects.
- Rechecked Xaero locally: `MinimapWriter` maintains separate `loadingCaving`/`loadedCaving` state and limits each write loop by both `tilesToUpdate` and a nanosecond `timeLimit`; `MapWriter` uses the same bounded tile/time pattern. PASS123 moves SimpleMap's retained Cave importer closer to that single-budget ownership model.
- No full Gradle build was run in this environment. Static source checks and Java parser/front-end probes are used for patch validation; Minecraft/NeoForge dependencies are not bundled with the supplied source workspace.

PASS122

- Removed the remaining deep-ocean chunk barcode at its source: exact basin-floor texture now fades out smoothly from 2 to 8 blocks of water depth, while shallow shore detail remains exact and biome water tint stays per-column. This prevents chunk-local seabed variation from being amplified across an otherwise flat ocean.
- Fixed Cave native-region handoff ownership. PASS121 telemetry showed 3,111 foreground handoff rejects and every sampled rejection had `planner_owned=false`; region-projected pages were being discarded even while their exact PageRequest was still live, then rebuilt through the generic exact path. Handoff now uses the same PageRequest lease as cancellation/publication and retains the existing source/projection revision fences.
- Coalesced incremental Cave minimap publication in four-child batches (75 ms maximum hold, six-child cold first batch). A 64x64 page and all mip levels are no longer regenerated/uploaded for every single arriving 16x16 child.

# Simple Map development changelog

Newest changes are kept at the top. This file replaces the old per-pass diagnosis/static-check files.

## PASS121 — Xaero water-overlay parity + retained-minimap publication fix + no-op Surface transaction suppression

- Re-audited the supplied PASS120 source, the 18:47 gameplay telemetry, the ocean screenshot and both Xaero codebases instead of extending the PASS119/120 assumptions. The screenshot has a dominant ~16-pixel horizontal/vertical phase at ~1x, so the artifact is tied to Minecraft chunk/subtile cadence, not random water texture noise.
- Ruled out the PASS120 sampler theory as the root cause. SimpleMap exact Surface now already matches Xaero World Map `RegionTexture`: `GL_LINEAR` minification and `GL_NEAREST` magnification, with one-pixel atlas gutters. Xaero `BiomeColorCalculator` also uses the same radius-1 centre + four-orthogonal-neighbour blend already used by SimpleMap. Those two paths are therefore kept, not rewritten again.
- Found a real remaining Xaero mismatch in water composition introduced by PASS119. Xaero `MapPixel` gives liquid RGB one fixed vanilla alpha of 191/255; `Overlay.opacity` separately accumulates depth up to 15 and only reduces the sunlight/brightness reaching the opaque basin floor. PASS119 added a second exponential `waterDepth -> alpha` ramp toward 0.92, so adjacent one-block depth changes modified both floor lighting and blend weight and amplified the chunk-aligned floor field. PASS121 restores fixed 191/255 water alpha and keeps depth only in the capped floor-light term. This is the first pass that combines the v5 connected-water/basin-floor source migration with Xaero-correct final water compositing.
- Bumped the derived Surface/Cave branch disk cache from v13 to v14 so coarse textures generated with the regressed PASS119/PASS120 water formula cannot survive beside corrected exact pages. The `.smdat` source format remains v5 because the stored water geometry is still valid.
- The minimap did not die through an FBO allocation/GL failure. In all 207 GAME telemetry samples `minimap_fbo_disabled=false` and fallbacks remain zero, but `minimap_fbo_redraws` reaches 13 and then stays exactly 13 for the final 137 GAME samples while reuse/coalescing keeps increasing. That proves the renderer froze a valid old front target and stopped publishing successful loading-target redraws.
- Root cause was the retained handoff gate. A same-authority redraw first copies the complete previous front target into the back FBO, then overwrites newly available current pages; that composite is already visually complete. PASS120 nevertheless rejected the swap whenever any known Surface M4 root/Cave branch in the target was not GPU-resident. One unrelated cold hierarchy node could therefore hold the minimap on frame 13 indefinitely. PASS121 publishes a successful redraw immediately when a compatible retained underlay was drawn, while true cold projection/scale/large-teleport redraws still require hierarchy coverage before replacing the last-good frame.
- Avoids the expensive full root/branch coverage walk on ordinary retained streaming redraws. The readiness walk now runs only for genuinely cold redraws, closer to Xaero MinimapWriter's `loadingBlocks -> loadedBlocks` transaction model.
- Found a separate Surface publication-amplification bug behind the new run's 4,084 `SURFACE_PAGE_ATOMIC_REFRESH` events and 2,616 Surface batch submissions/preparations. `Region.finishSurfaceChunkTransaction()` returned the chunk's old positive revision even when an already-complete 16x16 chunk had just been rescanned byte-identically. The outer manager interpreted every no-op scan as new source authority, dirtied the exact page/halo and restarted downstream exact/LOD/GPU work.
- Added per-chunk transaction dirty state. Pixel/tint changes mark the current private transaction dirty; `finishSurfaceChunkTransaction()` now returns zero when neither content nor completion changed, so the outer manager performs no persistence/page/work-graph publication for a no-op rescan. Newly acquired completion still receives a new revision even if retained pixel bytes compare equal, preserving the v4->v5 migration contract.
- Cave was not broadly rewritten again. The supplied run still has 1,342 `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` events, but this is already far below the 4,368 recorded in the prior PASS120 diagnosis and the current AUTO path records only one warm-source wait followed by one display latch. The dominant regressions in this run are the frozen retained minimap and Surface republish churn, so this pass fixes those evidenced faults rather than changing Cave source ownership blindly.
- Added/updated dependency-free guards for retained minimap publication, no-op Surface transaction publication and Xaero-stable water composition. Static lexical/delimiter checks and parser-front-end checks report no Java syntax/edit-artifact errors. No Gradle build was run because the supplied source bundle contains only `gradlew.bat` and no Gradle wrapper JAR/Unix launcher in this environment.

## PASS120 — Xaero exact-Surface minification + source-only Cave loading canvas + readiness-gated AUTO handoff

- PASS119 did not materially change the visible ocean artifact. The supplied ~1.0x Surface sample is overwhelmingly exact (`last_render_exact=118`, `last_render_branch=8` at scale 1.0037), so the previous water-depth/LOD work was not the dominant path. Image analysis also shows the dark blue/purple dash field has a strong 16-pixel phase, matching Minecraft chunk/subtile cadence rather than natural seabed geometry.
- Traced the final exact-Surface path to `SurfaceLeafAtlas`: both exact color and glow atlases still used `GL_NEAREST` for minification. At fractional scales just below/around 1.0x this point-samples high-frequency water/seabed detail and aliases the 16-block cadence into the repeated barcode visible in the screenshots. Rechecked Xaero World Map `RegionTexture.setupTextureParameters()`: magnification is `GL_NEAREST` (9728) but minification is explicitly `GL_LINEAR` (9729). SimpleMap exact Surface leaves now use the same LINEAR-min / NEAREST-mag pair; Cave exact/branch atlases keep their existing hierarchy-owned nearest semantics.
- Applied the same Xaero sampling pair to the legacy 512x512 exact Surface fallback texture so a not-yet-promoted leaf cannot reintroduce the nearest-minification barcode underneath the retained page table. Overview/LOD textures remain prefiltered and nearest, because their pixels already represent reduced density. No Surface source/cache format bump is needed for a sampler-only fix.
- PASS119's AUTO preparation still violated the `loadingCaving`/`loadedCaving` split it was intended to emulate. `prewarmAroundPlayer()` registered an 11x11 Cave rectangle as the real `MINIMAP` viewport while Surface was still displayed. The new run records 4,368 retained-region foreground rejects; 3,135 are `FULL + MINIMAP + planner_owned=false + projection_owned=true`. That is source work masquerading as presentation ownership.
- Replaced AUTO preparation with an allocation-free, source-only 5x5 centre-out warmup. It creates finite dense-tile transactions without any MINIMAP `VisiblePlanner`, foreground region lease or broad render rectangle. The actual minimap lane is registered only after Cave becomes the displayed projection.
- Added a compact hidden loading canvas on the BACKGROUND lane. Once the centre live tile exists, only the ~5x5-chunk centre footprint requests exact Cave pages while Surface remains visible, and normal bounded upload budgets stage them incrementally. This follows Xaero more closely: `loadingCaving` is written before `loadedCaving` is swapped instead of making the first visible Cave frame start from a cold exact/GPU state.
- AUTO entry is no longer time-only. After the existing one-second roof hysteresis, Surface stays displayed until the centre chunk plus four cardinal neighbours have fresh LIVE Cave tiles and the centre 64x64 projection has reached a resident exact page. Added `CAVE_AUTO_WAITING_WARM_SOURCE` telemetry for this state. A two-second hard ceiling prevents an unavailable background warmup from trapping AUTO on Surface forever. Briefly slow Cave generation therefore delays the invisible loading canvas, not the player's displayed frame.
- The supplied PASS119 run confirms the pacing problem is still projection-specific: gameplay Surface averages ~8.21 ms/frame, Layered Cave ~9.77 ms and Full Cave ~10.27 ms; Layered reaches ~19.83 ms and Full ~17.57 ms. End-of-run pressure still includes 5,811 branch GPU reservation denials, 477 live Cave requests and 517 REQUESTED exact states older than five seconds. PASS120 deliberately attacks the AUTO cold-start/false-foreground source of that churn rather than raising worker counts or GPU budgets.
- Cave exact request ownership now uses a Cave-specific 2 s MINIMAP lease instead of the generic 10 s lane TTL. The old value retained several seconds of travelled-away minimap pages as high-priority exact/branch demand, explaining why the run could end with 477 live Cave requests despite a compact current viewport. Fullscreen/background TTLs are unchanged.
- Removed a duplicated unreachable `return` line in `CaveDisplayScheduler.DeferredEntry.current()` encountered during the source audit.
- No Gradle build run.

## PASS119 — Xaero-style AUTO Cave display latch + compact minimap working set + Surface water-source migration

- PASS118 keeps Cave source correctness substantially healthier (`CAVE_PAGE_NO_SOURCE=0`, 1,278 chunk-coherent partial publications, 243 child-ready admissions), but gameplay telemetry isolates a presentation/work-lifetime problem. `MINIMAP_SURFACE` averages about 8.08 ms/frame, `MINIMAP_CAVE_LAYERED` about 9.07 ms, and `MINIMAP_CAVE_FULL` about 10.76 ms with a 24.88 ms observed maximum. In only 41 Layered gameplay samples the pipeline accumulated 675 exact-build discards, 779 completed-task discards and 1,014 branch GPU reservation denials. Surface is comparatively stable, so adding workers would amplify stale work rather than fix pacing.
- Rechecked Xaero Minimap `MinimapWriter`: AUTO detection produces a candidate `cavingDestination`, while `loadingCaving` and `loadedCaving` are separate states. Its profiled `CAVE_MODE_TOGGLE_TIMER` defaults to 1000 ms, and `beforeWriting()` keeps the previous displayed map during that window. When entering Cave it further clips the writer to a compact centre neighbourhood (`maxDistance=1` map-chunk) instead of rebuilding the complete render-distance square. Writer progress is bounded both by `tilesToUpdate` and an elapsed-nanosecond deadline.
- SimpleMap AUTO previously entered after only 2 ticks (~100 ms). The renderer changed Surface->Cave on the same revision that started broad live Cave admission, exact builds, branch derivation and GPU publication. The supplied run contains 28 gameplay projection transitions, with repeated Surface<->Cave oscillation every few seconds around roofs/openings. AUTO entry now requires 20 stable ticks (~1 s), matching Xaero's default display timer; exit keeps the existing 20-tick hysteresis. Layered automatic Top-Y retarget stability also increases from 8 to 20 ticks to stop stair/player-Y jitter from invalidating retained projections.
- Added an explicit AUTO preparation state. Roof detection becomes visible to the Cave writer immediately through `CaveMode.shouldPrepare()`, but renderers continue to use `CaveMode.isActive()`. During the one-second latch SimpleMap keeps Surface displayed and prewarms only a centre 11x11-chunk Cave source window under a 0.35 ms deadline. AUTO state changes now start `CaveModeTransitionPolicy`, whose first visible Cave frames cap source continuation at 0.30 ms.
- Removed another gameplay amplification source: the 20 Hz Cave fallback writer no longer admits the entire render-distance square; it owns only a compact +/-4 chunk fallback. The real MINIMAP presentation lane now uses the actual renderer viewport plus a one-chunk halo, clipped to loaded render distance. Durable vertical archive capture remains independent in packet/background/saved-world paths. This follows Xaero's distinction between a small active minimap writer and retained/world-map source data.
- Reduced healthy client-thread Cave source budget from 2.0 ms to 0.85 ms. The render-frame foreground writer gives Cave at most 0.65 ms / 40% of its credit, with Surface history continuing from the remainder. Exact/branch/GPU stages keep their own independent budgets; the intent is to prevent AUTO Cave from stacking several foreground budgets onto one physical frame.
- The Surface water screenshot at ~1.0x is mostly exact pages (`last_render_exact=60`, `last_render_branch=4`), proving PASS118's LOD supersampling could not remove the persistent stripe field by itself. The canonical live-water geometry was changed in PASS117 (connected highest water surface + physical basin floor), but `.smdat` stayed format v4, so already-complete v4 ocean chunks were never rescanned and continued supplying the old waterDepth/floorY field forever.
- Surface `.smdat` semantic version is now v5. Loading any older file preserves its pixels as last-good data but clears completion only for chunks containing non-glowing fluid, allowing the ordinary live/Anvil transaction writer to rebuild water columns with the current canonical scanner while leaving land completion untouched. Added `SurfaceChunkCoverage.clearComplete()` for this targeted migration.
- Deep-water composition now better approximates Xaero's accumulated vertical overlay/light loss. Shallow water retains the vanilla-like 191/255 overlay, while floor transmittance decays smoothly with water depth toward a bounded 0.92 water alpha. This suppresses high-frequency seabed remnants in deep oceans without blurring neighbours or changing biome-water hue.
- Bumped the shared Surface/Cave LOD branch disk cache from v12 to v13 because derived water colours changed; old coarse branch textures cannot override repaired exact water after migration.
- No Gradle build run.

## PASS118 — Cave child admission + Surface supersampled water LOD + minimap branch pacing

- PASS117 source correctness is largely holding: the new run contains `CAVE_PAGE_NO_SOURCE=0`, 636 chunk-coherent partial publications and 330/330 successful Cave archive page rehydrates. The remaining missing/delayed Cave areas are therefore primarily an admission/presentation scheduling problem rather than proof that the Minecraft chunk source is absent.
- Found a hidden 16/16 barrier one layer above PASS116's partial projection fix. `CaveRegionProjectionService` could publish individual complete 16x16 children, but `CaveNativeRegionImportService` only admitted a 64x64 page after all sixteen central chunks were `caveArchived`. Surface publishes one completed Minecraft chunk immediately, so the two views could read the same world source yet have different visible readiness. Added child-progressive page admission: one durable generated child is enough to request projection; only 256/256-known children enter coverage, unresolved siblings stay transparent and later source revisions expand the same retained page. Added `CAVE_NATIVE_REGION_CHILD_READY` telemetry.
- Rechecked Xaero's `MapTileChunk.updateBuffers()`: unloaded child `MapTile`s are skipped individually rather than acting as a barrier for loaded siblings. PASS118 now applies that ownership rule at the importer as well as at the projector, removing the last known 4x4-page gate that could hide already-generated Cave chunks.
- The PASS117 water-color formula was not the main visual path in the supplied wide-zoom screenshot. Surface M4 direct LOD still point-sampled exactly one centre column from every 8x8 world-block cell. Regular seabed/kelp/sand detail therefore aliased into the long vertical/horizontal dash pattern even though the exact water composer had changed.
- Replaced the M4 8-block point sampler with a bounded 4-block supersample source (128x128 samples per 512x512 region). The worker styles this grid and linearly averages each 2x2 group into the final 64x64 branch texel. This keeps the direct retained underlay cheap compared with a full 512x512 projection but removes the deterministic one-sample-per-8x8 alias responsible for the water barcode.
- `SurfaceLodTree` higher-level reduction also no longer chooses one real child colour nearest the mean. It now publishes the arithmetic mean of covered child texels. Xaero's branch path renders child textures into a smaller branch texture while the source texture uses linear minification; arithmetic averaging is the retained CPU equivalent and prevents fine seabed/foliage patterns from surviving as coarse stripes.
- Bumped shared LOD branch disk-cache version 11 -> 12 so old point-sampled/medoid Surface branches cannot mask the new water/LOD code after restart. Exact `.smdat` Surface source remains valid because its per-column geometry did not change.
- Frame-pacing diagnosis: this run peaked at ~69.8 ms frame average, ~2.52 GiB/s allocation, ~4.01 GiB heap and a 336 ms sampled GC interval. Branch GPU reservation denials reached 19,573; 14,255 of those happened specifically in `GAME + LAYERED`. This is not a general GPU starvation signal: Cave minimap exact publication was consuming the per-frame ledger before the branch underlay got its upload attempt, while MINIMAP ownership propagated unnecessarily into higher ancestors.
- Cave minimap now records the density level it actually renders and propagates MINIMAP foreground ownership only up to that level; higher ancestors remain background unless the fullscreen viewport independently needs them. This prevents one minimap leaf update from promoting the complete L2..Ln ancestor chain.
- When gameplay/minimap needs branch coverage, one bounded branch upload is now attempted before exact publication while the GPU frame ledger is still empty. The old post-exact branch retry is skipped for that minimap frame, preventing the same branch from repeatedly reaching an already-spent GPU budget. Fullscreen/far-zoom branch-first behavior is unchanged.
- Remaining pressure after these targeted fixes is still visible in the baseline: 12,215 Surface atomic refreshes, 7,720 MCA proto deferrals, 792 stale Cave results, 24,248 branch updates and branch queue age up to ~18.88 s. PASS118 deliberately does not enlarge thread pools or GPU budgets; the next log should show whether child admission and minimap branch ownership remove the correctness holes and the dominant `GAME + LAYERED` denial storm before deeper allocation/refresh work.
- Static scan passes for all 248 production Java files with zero delimiter/lexical errors and zero backslashes outside literals/comments. `javac --release 21 -proc:none` front-end probing found no illegal-character/parser/statement/signature errors in the modified units before the expected missing Minecraft/NeoForge/FastUtil/Gson classpath errors. No Gradle build run.

## PASS117 — fair native-region scheduling + Xaero water opacity + Cave source parity

- PASS116 validation confirms the previous correctness fixes are active: `CAVE_PAGE_NO_SOURCE` is now 0, `CAVE_LAYER_BAND_ALIAS_BYPASSED` fired 480 times during exact Top-Y retargets, and `CAVE_REGION_PARTIAL_CHILD_PUBLICATION` fired 112 times. Cave source absence is therefore no longer the main explanation for the remaining black/mixed areas.
- The current fullscreen Cave plan reached 336 foreground pages (`504` source pages including halo). At the screenshot location, native region `-6,-14` started at 1/64 ready pages around 688.5 s and took until about 696.5 s to reach 64/64 visible pages; neighbouring `-5,-14` did not receive a substantial 48-page submission until that first region was nearly drained. This creates the large rectangular Cave-ready area beside Surface/blank seen in the screenshot even though Minecraft chunks already exist.
- Root cause was source-admission fairness, not missing chunks: the importer had 32 global source slots but `SOURCE_SLICE=16` and strict region-priority refill, allowing two leading native regions to monopolize all decode leases. Reduced the per-region slice to 8 and sort same-lane candidates by current in-flight count before region priority, so free slots spread across the visible native regions instead of repeatedly refilling the first region. This follows Xaero's independent `MapTile`/child progress more closely.
- Surface-water diagnosis found a second Xaero mismatch. SimpleMap stored the real basin floor and water depth correctly, but final color composition used one fixed water alpha over a fully lit floor. In deep oceans this left roughly one quarter of every gravel/plant/height variation visible at full brightness, producing the regular dash/grid field. Xaero keeps liquid transparency fixed at 191/255 while accumulated water opacity consumes the sunlight that reaches the floor.
- Surface water now uses exact per-column `MapBlockData.waterDepth`, clamps accumulated opacity to 0..15, reduces only basin-floor illumination using Xaero's `lightMin=9` brightness form, then composites one stable biome-water tint at alpha `191/255`. No 5x5 neighbour blur or synthetic fluid noise is used. Deep water therefore converges toward a stable water sheet while shallow water still reveals its floor.
- Live Surface now mirrors the world-save path when a partially updated heightmap or waterlogged plant resolves below the actual connected liquid surface. It walks upward through the connected water column before storing `topY` and calculating depth, preventing kelp/seagrass columns from becoming false shallow-water stripes.
- `SURFACE_MCA_PROTO_DEFERRED` rose to 6,412 although every deferral nudged the live writer. The 30-second disk retry timer also removed those cells from normal source selection, so the importer could fail to notice that the canonical live writer had already completed the Surface chunk. Added a deferred-cell set with a bounded 32-cell round-robin ACK slice on each importer pump; once `MapManager.isChunkSurfaceComplete()` becomes true, the source cell is ACKed immediately without reopening Anvil. The cursor prevents thousands of proto cells from turning the recovery check into a new O(N) pump cost. Added `SURFACE_PROTO_LIVE_WRITER_ACK` telemetry.
- Full-Cave live scanning still used `MOTION_BLOCKING_NO_LEAVES` plus a three-consecutive-solid roof heuristic while decoded/archive projection used Xaero-style first-real-terrain entry from `WORLD_SURFACE`. Thin one/two-block roofs could therefore make the same chunk project differently depending on whether it came from live memory or Anvil. The live scanner now uses `WORLD_SURFACE` and the same terrain-entry predicate as the canonical Cave semantics.
- Cave archive material handling is also normalized: the canonical live tile scanner, decoded Anvil archive and legacy world-save decoder now store dense/raw material colors instead of mixing pre-lit and raw source authorities. Water-over-floor archive composition now keeps Xaero's fixed liquid alpha and encodes water-column opacity as floor-light loss instead of changing water RGB/alpha with depth. Final depth/light/style remains the responsibility of `CavePageStyler`.
- Derived/raw Cave cache namespaces were invalidated because both vertical geometry and water material semantics changed: CVR region v5 / snapshot v8, CVD region v7 / tile v8, CIMG v8, Cave projection style v19, Cave LOD namespace `cave_v11_`; shared LOD branch cache is v11 for the new Surface-water result. First access after this pass is expected to rebuild affected Cave/LOD cache data.
- Performance is improved from the older handoff/source failures but still not clean: this run ended with 3,512 branch GPU reservation denials, branch queue max about 23.25 s, 13,946 Surface atomic refreshes versus 1,594 first GPU-ready pages, and heap peak about 4.01 GiB. Those remain work-amplification/residency targets after the new fairness/water/cave-geometry correctness baseline is validated.
- Static source scan passes for all 346 Java files with zero delimiter/lexical errors and zero backslashes outside literals/comments. A `javac -proc:none -XDshould-stop.at=PARSE` front-end probe on modified production units found no parser/illegal-character errors before expected missing Minecraft/NeoForge/FastUtil symbol errors. No Gradle build run.

## PASS116 — exact Layered identity + chunk-coherent Cave partial publication

- PASS115 validation is positive: `CAVE_PAGE_NO_SOURCE` fell from 440 to 0, `CAVE_RESULT_STALE` fell from 1,662 to 687, and Cave foreground handoff rejects fell from 2,045 to 1,081. Branch coalescing also reduced branch GPU reservation denials from 5,854 to 1,867 and exact-build discards from 4,012 to 2,463.
- The remaining Cave residency telemetry exposed a different correctness fault: 93 `CAVE_INDEXED_SOURCE_WAITING_RESIDENCY` events remained, and 72 of them had both `archive_load=false` and `display_load=false`. 67 Layered waits shared the exact same synthetic source revision `-6099323197526026806`, proving the scheduler was seeing revision history rather than a loadable source.
- Root cause: `DenseCaveTileKey` intentionally stores the normalized 16-block band, while `DenseCaveTile` stores the exact projection Top-Y. The old `projectionPageRevisions` counter was also band-keyed, so a previous Layered projection such as Top-Y=-13 could leave a non-zero source revision for Top-Y=-1 even when no tile/archive for -1 was resident or loadable.
- Removed the historical projection-page mutation counter from source identity. `getPageRevision()` now fingerprints only exact resident dense tiles, exact known-absence leaves, and persistent compact-archive identity. A same-band tile with the wrong `projectionTopY` contributes nothing to the current Layered source.
- Exact Layered Top-Y is now mixed into the final page revision independently of the normalized band. Style-independent vertical archive data may be identical for Top-Y=-13 and Top-Y=-1, but the projected visual is not; those projections can no longer share a source revision accidentally.
- `getDisplayTile()` and page/range display-cache admission now reject same-band/wrong-Top-Y dense tiles as current source. Such a tile remains a retained presentation/cache object but cannot block raw/vertical archive recovery for the exact requested slice. Added throttled `CAVE_LAYER_BAND_ALIAS_BYPASSED` telemetry.
- `resolvePage()` no longer copies pixels from a mismatched Layered dense tile into the new projection's `knownRows`. The previous GPU page remains last-good during a Top-Y transition, while the new source transaction stays exact instead of permanently splicing two cave heights into one 64x64 page.
- Removed the remaining all-16-child publication barrier from native Cave region projection. A page can now publish when at least one complete 16x16 Minecraft child is available. Arbitrary sparse columns are masked out: only children whose full 256 columns are known enter the published `knownRows`, matching Xaero's `MapTileChunk.updateBuffers()` rule that each loaded child tile can update independently.
- Partial native Cave pages now carry their real `knownColumnCount`/`complete` state into `CaveLodTree` and exact handoff instead of being hard-coded as 4096/4096 complete. This is especially important at wide fullscreen zoom where exact leaves are intentionally disabled and branch coverage is the visible authority.
- Partial pages are not persisted into CIMG because that format has page-level presence but no per-column coverage mask. Only a complete 64x64 projected page is staged into the region-image transaction; partial coverage remains retained in the live LOD/presentation path and expands when source revisions advance. Added `CAVE_REGION_PARTIAL_CHILD_PUBLICATION` telemetry.
- Surface is no longer the dominant correctness fault in this run, but exact refresh amplification remains: 10,457 atomic refreshes for 1,197 first-GPU-ready events, while `SURFACE_MCA_PROTO_DEFERRED` rose to 3,826. All proto deferrals reported `live_writer_nudged=true`; the live `ChunkStatus.FULL` recovery path remains active. Surface branch pressure improved materially, so this pass does not re-open the atlas/LOD lifetime logic while Cave source identity is being corrected.
- Current runtime still reaches about 3.96 GiB heap peak and high transient allocation (roughly 1.0–1.1 GiB/s in GAME Cave modes, with higher bursts). Allocation/work amplification remains a performance target after Cave coverage correctness is revalidated.
- Static lexical/delimiter scan passes for all 248 production Java files. A `javac --release 21 -proc:none` parser/front-end probe on the three modified units reports no syntax/illegal-character errors before expected missing Minecraft/NeoForge/FastUtil classpath errors. No Gradle build run.

## PASS115 — resident Cave authority + demand archive paging + Surface branch coalescing

- PASS114 validation confirms the source/presentation split worked: `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` fell from 49,244 in the previous run to 2,045 in this 10.24-minute run. The remaining rejects are ordinary planner-rebase races rather than the old source-halo handoff storm.
- The next correctness fault was persistent-index/resident-source conflation. The run emitted 440 `CAVE_PAGE_NO_SOURCE` events; 203 were labelled authoritative even though the build returned zero known columns, and 124 unique no-source pages never reached a later successful GPU publication before the session ended.
- Startup replay indexed 43,076 compact Cave tiles while `CaveArchiveV2Service` can retain only 32,768 tiles / 192 MiB. PASS114.1 therefore decoded all archive records into RAM, immediately LRU-evicted thousands, but kept their indexed fingerprints/completeness bits. `CaveTileRepository.getPageRevision()` could still advertise those evicted records as source authority while `CaveProjectionServiceV2` requires an actually resident `CompactCaveTile`.
- Split persistent Cave identity from residency explicitly. Startup SMR2 replay now installs archive fingerprints/completeness through `CaveArchiveV2Service.index(...)` without pretending every decoded archive is resident. This follows Xaero's `MapTile.isLoaded()` distinction: persistent/cache identity may exist while publication still waits for loaded source.
- Added random-access `loadCaveArchivePage(...)` for one 64x64 map page (4x4 chunks) from its single SMR2 region container. `CaveTileRepository` deduplicates page rehydrate requests and restores only indexed-but-evicted compact records required by the visible page. Byte-identical rehydration preserves the existing source fingerprint, so residency recovery does not create another stale-revision cascade.
- Exact Cave admission now checks actual resident/dense/raw/known-absence source separately from the indexed source revision. Indexed-only pages request dense-cache/archive residency first instead of launching a zero-column exact build. `noSourceRevision` suppression is allowed only when all sixteen central children have complete *resident* projection authority or explicit absence.
- `CaveWorldSaveReader` now asks for indexed SMR2 page residency before falling back to Anvil decoding, including remote-client cache replay where server Anvil files are unavailable. Full Cave archive coverage uses `fullProjectionCoverage()` rather than the unnecessarily stronger arbitrary-Layered `completeCoverage()` gate, avoiding redundant Full-Cave redecodes of complete-truncated vertical archives.
- Surface remains the main amplification bottleneck: 10,938 atomic refreshes touched only 718 unique pages (mean 15.23 refreshes/page, median 9, worst 109), while branch GPU reservation denial reached 5,854 and branch queue max reached 14.87 s. Exact Surface publication remains immediate, but factor-2 branch derivation now coalesces transient leaf revisions behind a short quiet window (40 ms foreground / 80 ms background, hard max 160/300 ms). The newest retained leaf revision wins, reducing stale branch derivation and GPU pressure without delaying exact map pixels.
- Branch queue telemetry now counts actual leaf queue insertions instead of every source replacement that was already coalesced under the same retained key.
- `SURFACE_MCA_PROTO_DEFERRED` remains high (2,848); PASS114's live `ChunkStatus.FULL` writer nudge is retained. This is still a secondary recovery/performance issue, not treated as proof that the generated Minecraft chunk is absent.
- Added `CAVE_ARCHIVE_PAGE_REHYDRATE_REQUEST`, `CAVE_ARCHIVE_PAGE_REHYDRATE_READY`, `CAVE_ARCHIVE_PAGE_REHYDRATE_MISS`, `CAVE_INDEXED_SOURCE_WAITING_RESIDENCY`, and `CAVE_ARCHIVE_PERSISTENCE_INDEX_READY` telemetry for the next validation run.
- No Gradle build run.

## PASS114.1 — compile hotfix

- Fixed an accidental literal `\n` escape emitted into `CaveNativeRegionImportService.java` while patching PASS114. The `foregroundMask` expression now uses a real Java line break before `& sourceMask`.
- Scanned Java source for backslash characters outside strings/comments to catch the same patch-artifact class elsewhere.
- No Gradle build run.

## PASS114 — Cave source/presentation split + stale-lease revocation

- PASS113 validation exposed a handoff storm: `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` fired 49,244 times. 31,062 Full-Cave rejects still had valid projection ownership but no renderer planner ownership, while 18,182 Layered rejects had already lost both planner and projection ownership.
- Root cause: fullscreen world-save enumeration intentionally expands the source window by a two-page sticky halo, but PASS113 passed that expanded source plan to `CaveRegionProjectionService` as foreground presentation demand. `UnifiedCaveTextureManager` correctly rejected those support-only pages, then PASS113 re-offered them every 250 ms.
- Split native Cave demand into two explicit plans: `sourcePagePlan` owns Anvil/archive decoding and the sticky support halo; `foregroundPagePlan` owns only pages the active renderer planner can present. Foreground masks are intersected with admitted source masks.
- The legacy world-save reader and the canonical `WorldSaveProjectionPipeline` now preserve the pre-halo viewport separately and pass both plans to the native-region importer. Minimap keeps its renderer-owned halo aligned; fullscreen's extra two-page halo is source-only.
- Added `CAVE_SOURCE_PRESENTATION_SPLIT` telemetry so the next log reports source pages, foreground pages and source-only support pages directly.
- Changed a rejected retained-page handoff from endless re-offer semantics to stale-lease revocation. The immutable projected page remains cached; only the obsolete foreground bit/offer/ACK state is cleared. A later real viewport lease can promote the retained page again without rebuilding it.
- Surface Anvil recovery now treats `Status != FULL` on disk as non-authoritative when the same chunk may already be a live `ChunkStatus.FULL` client chunk. It nudges that exact chunk back into the canonical live Surface writer instead of relying solely on the 30-second disk retry.
- This follows Xaero's ownership split more closely: neighbour/source data may be retained to support future writes, while only actual loaded `MapTile` children are copied into `MapTileChunk.updateBuffers()`; support data is not itself a render-publication obligation.
- Remaining observed pressure after PASS113: 18,829 Surface atomic refreshes, 769 stale Cave build results, branch queue max 11.86 s and 5,334 branch GPU reservation denials. These are left as the next amplification/LOD target after verifying that the handoff storm and missing-Cave holes collapse.
- No Gradle build run.

## PASS113 — retained Cave handoff ACK + re-offer

- Fixed a one-shot producer/consumer race in `CaveRegionProjectionService`: putting a cached projected page into `readyPages` no longer marks that source revision as permanently delivered.
- Added an explicit foreground ACK contract. The projection service suppresses a cached page only after `UnifiedCaveTextureManager` has accepted the exact revision into the retained LOD/presentation path.
- A page polled during a fullscreen mode switch, Layered Top-Y retarget, or viewport rebase is intentionally not ACKed. The immutable cached projection is re-offered after a 250 ms anti-duplicate window on the next foreground lease pulse.
- Added `CAVE_REGION_FOREGROUND_HANDOFF_REJECTED` telemetry for planner/projection ownership races and changed frontier telemetry from one-shot `released` accounting to `offered/acked` accounting.
- Foreground handoff lane now follows the current native-import lease instead of retaining a historical stronger lane, preventing cached pages from being repeatedly offered to an inactive minimap/fullscreen planner.
- New PASS112 log proves the missing Full Cave stripe is downstream of source generation: region `-2,-1` reported `ready_pages=64/64`; 9 cached Full pages were immediately marked released, only 55 pages were freshly projected/saved, and pages such as `-16..-13,-3` had Full archive authority but never reached a Full GPU swap during the run.
- This moves Cave publication closer to Xaero's retained `MapTile`/`MapTileChunk` model: source pixels remain authoritative and can repopulate presentation after a transient consumer rejection instead of being consumed as a one-shot message.
- PASS112's non-authoritative Layered no-source retry remains active; all 124 `CAVE_PAGE_NO_SOURCE` events in the new log were Layered/incomplete rather than proven absent source.
- No Gradle build run.

## PASS112 — atlas lifetime fencing + Cave no-source recovery

- Fixed the wide-zoom Surface race where an evicted branch/region atlas slot could be reused while an immutable render plan still contained the old raw UVs. This was capable of drawing terrain from one coordinate at another coordinate (the rectangular “wrong map piece” corruption seen at 0.10x zoom).
- Added frame-boundary quarantine before reusing Surface factor-2 branch slots and M4 region-LOD slots. Runtime eviction now invalidates render-plan topology before those slots can be recycled.
- Added one-retirement-per-level/frame guards so fencing cannot turn atlas pressure into cascade eviction.
- Applied the same lifetime rule to Cave branch atlases because Cave branches also use raw atlas UVs in cached plans.
- Fenced published Cave exact slots until the GPU page-table swap; unpublished reservation failures still release immediately.
- Fixed Cave negative-source caching: an incomplete zero-column Layered/Full probe is no longer treated as permanent “no source” merely because its source revision is unchanged. Same-revision suppression is allowed only after the repository proves all 16 children archived or explicitly absent.
- PASS111 partial Surface publication is confirmed active in the new runtime (`SURFACE_PAGE_GPU_READY` contains masks below `0xffff`).
- New runtime still shows heavy Surface refresh churn and Cave source churn; those remain performance targets after correctness is rechecked.
- No Gradle build run.

## PASS111 — partial Surface admission + retained-source recovery

- Removed remaining 16/16 page-admission barriers; coherent partial 64x64 Surface pages can publish with their real 4x4 coverage mask.
- Partial CPU pages can restore after atlas rebase/teleport instead of requiring 4096/4096 columns.
- Foreground demand can requeue a live `ChunkStatus.FULL` chunk into the canonical Surface writer when retained authority missed it.
- Foreground unchanged-partial materialization retry reduced to 180 ms while background/prefetch keeps the longer anti-churn fence.
- Retained Surface authority is preferred over mutable legacy `MapManager.Region` availability.

## PASS110 — local publication + canonical Cave/Surface reconciliation

- Removed the global Cave fullscreen publication wavefront/head-of-line barrier.
- Surface body publication no longer requires an impossible 3x3 FULL-neighbour set at the render-distance edge.
- Introduced coherent partial Surface page transactions instead of hiding 15 valid children because one child is unavailable.
- Removed legacy-region gating from Surface neighbour repair after teleport/cache replay.
- Unified Full Cave vertical semantics across live dense, live archive, decoded Anvil, and legacy world-save paths using column-local Xaero-style terrain/cavity/floor selection.
- Invalidated old Cave cache formats after the projection-semantics change.

## Earlier development passes

PASS94–PASS109 diagnostics were previously stored as separate report/check files. They are intentionally no longer shipped as individual files; future work is prepended to this changelog only.
